import { Meta, Preview, Story } from '@storybook/addon-docs/blocks';
import {
  Button,
  Link,
  IconButton,
  IconGhostButton,
  IconLinkButton,
  IconOutlineButton,
  OutlineButton,
} from '@magnetis/astro-galaxy-components';
import { colors } from '@magnetis/astro-galaxy-tokens';
import { IconCalendar } from '@magnetis/astro-galaxy-icons';
import PreviewDivider from './components/PreviewDivider';

<Meta title="buttons" />

# buttons

#### Buttons define what action will occur when the user interacts with features and components of the platform. Buttons are used to initialize and dictate the actions on the interface, either in the background or foreground of an experience.

## primary buttons

Primary buttons are medium sized and have a colored background. Use these for the main action in a section or screen. You should display only one primary button at a time.

To create a primary button, import `Button` component and use `variant` prop with `primary.uranus` value. You can replace `uranus` with other set of colors.

Mouseover and click to view `:hover` and `:active` states.

<Preview>
  <Story name="primary buttons">
    <>
      <Button variant="primary.uranus">uranus</Button>
      <Button variant="primary.earth">earth</Button>
      <Button variant="primary.venus">venus</Button>
      <Button variant="primary.mars">mars</Button>
      <Button disabled>disabled</Button>
    </>
  </Story>
</Preview>

## outline buttons

Outline buttons can be used as secondary roles. Secondary buttons present secondary actions when there's already a primary action in the same section or screen.

To create a outline button, import `OutlineButton` component and use `variant` prop with `outline.uranus` value. You can replace uranus with other set of colors.

Mouseover and click to view `:hover` and `:active` states.

<Preview>
  <Story name="outline buttons">
    <>
      <OutlineButton variant="outline.uranus">outline uranus</OutlineButton>
      <OutlineButton variant="outline.earth">outline earth</OutlineButton>
      <OutlineButton variant="outline.venus">outline venus</OutlineButton>
      <OutlineButton variant="outline.mars">outline mars</OutlineButton>
      <OutlineButton disabled>disabled</OutlineButton>
    </>
  </Story>
</Preview>

## iconlabel buttons

Iconlabel buttons can be used for simple microinteractions and commands.

To create an iconlabel button, first create a regular `IconButton`, plus the `iconlabel` prop. Then, if you want the icon to the left of the label, add an icon component inside the button and before its inner text. See our Iconography to learn how to use Astro icons.

If you want the icon to the right of the label, add the `iconlabelRight` prop in addition to the previous props and place the icon component inside the button and after its inner text, instead.

Mouseover and click to view `:hover` and `:active` states.

<Preview>
  <Story name="iconlabel buttons">
    <>
      <IconButton iconlabel>
        <IconCalendar />
        iconlabel
      </IconButton>
      <IconButton iconlabel iconlabelRight>
        iconlabel right
        <IconCalendar />
      </IconButton>
      <IconButton iconlabel disabled>
        <IconCalendar />
        iconlabel disabled
      </IconButton>
    </>
  </Story>
</Preview>

## outline iconlabel buttons

Outline iconlabel buttons are commonly used in microinteractions and secondary commands. Usage works the same as regular iconlabel buttons.

To create an outline iconlabel button import `IconOutlineButton` component and use `variant` prop with `outline.uranus` value. Then, add an icon component inside the icon button. See our Iconography to learn how to use Astro icons.

Mouseover and click to view `:hover` and `:active` states.

<Preview>
  <Story name="outline iconlabel buttons">
    <>
      <IconOutlineButton iconlabel>
        <IconCalendar />
        iconlabel
      </IconOutlineButton>
      <IconOutlineButton iconlabel iconlabelRight>
        iconlabel right
        <IconCalendar />
      </IconOutlineButton>
      <IconOutlineButton iconlabel disabled>
        <IconCalendar />
        iconlabel disabled
      </IconOutlineButton>
    </>
  </Story>
</Preview>

## ghost iconlabel buttons

Ghost iconlabel buttons are also used in microinteractions and smaller commands. Usage works the same as regular iconlabel buttons.

To create an ghost iconlabel button, first create a regular `IconGhostButton`, plus the `iconlabel` prop. Then, if you want the icon to the left of the label, add an icon component inside the button and before its inner text. See our Iconography to learn how to use Astro icons.

If you want the icon to the right of the label, add the `iconlabelRight` prop in addition to the previous props and place the icon component inside the button and after its inner text, instead.

Mouseover and click to view `:hover` and `:active` states.

<Preview>
  <Story name="ghost iconlabel buttons">
    <>
      <IconGhostButton iconlabel>
        <IconCalendar />
        iconlabel
      </IconGhostButton>
      <IconGhostButton iconlabel iconlabelRight>
        iconlabel right
        <IconCalendar />
      </IconGhostButton>
      <IconGhostButton iconlabel disabled>
        <IconCalendar />
        iconlabel disabled
      </IconGhostButton>
    </>
  </Story>
</Preview>

## icon buttons

Icon buttons can be used for simple microinteractions and commands.

To create an icon button, import `IconButton` component and use `variant` prop with `primary.uranus` value. Then, add an icon component inside the icon button. See our Iconography to learn how to use Astro icons.

To modify button size you may use `buttonSize` prop with `small`, `medium` or `large` value. Default value is `medium`.

Mouseover and click to view `:hover` and `:active` states.

<Preview>
  <Story name="icon buttons">
    <>
      <IconButton variant="primary.uranus">
        <IconCalendar />
      </IconButton>
      <IconButton variant="primary.earth">
        <IconCalendar />
      </IconButton>
      <IconButton variant="primary.venus">
        <IconCalendar />
      </IconButton>
      <IconButton variant="primary.mars">
        <IconCalendar />
      </IconButton>
      <IconButton disabled>
        <IconCalendar />
      </IconButton>
    </>
  </Story>
</Preview>

## outline icon buttons

Outline icon buttons can be used for simple microinteractions and commands.

To create an outline icon button import `IconOutlineButton` component and use `variant` prop with `outline.uranus` value. Then, add an icon component inside the icon button. See our Iconography to learn how to use Astro icons.

To modify button size you may use `buttonSize` prop with `small`, `medium` or `large` value. Default value is `medium`.

Mouseover and click to view `:hover` and `:active` states.

<Preview>
  <Story name="outline icon buttons">
    <>
      <IconOutlineButton variant="outline.uranus">
        <IconCalendar />
      </IconOutlineButton>
      <IconOutlineButton variant="outline.earth">
        <IconCalendar />
      </IconOutlineButton>
      <IconOutlineButton variant="outline.venus">
        <IconCalendar />
      </IconOutlineButton>
      <IconOutlineButton variant="outline.mars">
        <IconCalendar />
      </IconOutlineButton>
      <IconOutlineButton disabled>
        <IconCalendar />
      </IconOutlineButton>
    </>
  </Story>
</Preview>

## ghost icon buttons

Ghost icon buttons can be used for simple microinteractions and commands.

To create a ghost icon button import `IconGhostButton` component and use `variant` prop with `ghost.uranus` value. Then, add an icon component inside the icon button. See our Iconography to learn how to use Astro icons.

Mouseover and click to view `:hover` and `:active` states.

<Preview>
  <Story name="ghost icon buttons">
    <>
      <IconGhostButton variant="ghost.uranus">
        <IconCalendar />
      </IconGhostButton>
      <IconGhostButton variant="ghost.earth">
        <IconCalendar />
      </IconGhostButton>
      <IconGhostButton variant="ghost.venus">
        <IconCalendar />
      </IconGhostButton>
      <IconGhostButton variant="ghost.mars">
        <IconCalendar />
      </IconGhostButton>
      <IconGhostButton disabled>
        <IconCalendar />
      </IconGhostButton>
    </>
  </Story>
</Preview>

## icon link buttons

Icon link buttons can be used for menu items and simple interactions or commands.

To create an icon link button, import `IconLinkButton`. Then, if you want the icon to the left of the label, add an icon component inside the button and before its inner text. See our Iconography to learn how to use Astro icons.

If you want the icon to the right of the label, add the `iconlabelRight` prop in addition to the previous props and place the icon component inside the button and after its inner text, instead.

To create an icon link button with bold text, just add the `bold` prop.

Mouseover and click to view `:hover` and `:active` states.

<Preview style={{ backgroundColor: colors.moon900 }}>
  <Story
    name="icon link buttons"
    parameters={{
      backgrounds: [{ name: 'moon 900', value: colors.moon900, default: true }],
    }}>
    <>
      <IconLinkButton>
        <IconCalendar />
        icon link
      </IconLinkButton>
      <IconLinkButton bold>
        <IconCalendar />
        icon link
      </IconLinkButton>
      <IconLinkButton iconlabel iconlabelRight>
        icon link right
        <IconCalendar />
      </IconLinkButton>
      <IconLinkButton disabled>
        <IconCalendar />
        icon link disabled
      </IconLinkButton>
    </>
  </Story>
</Preview>

## sizes

Buttons can have different sizes. To modify button size you may use `buttonSize` prop with `small`, `medium` or `large` value. Default value is `medium`.

<Preview>
  <Story name="sizes">
    <>
      <Button buttonSize="small">small</Button>
      <Button>medium</Button>
      <Button buttonSize="large">large</Button>
      <PreviewDivider />
      <OutlineButton buttonSize="small">outline small</OutlineButton>
      <OutlineButton>outline medium</OutlineButton>
      <OutlineButton buttonSize="large">outline large</OutlineButton>
      <PreviewDivider />
      <IconButton buttonSize="small" iconlabel>
        <IconCalendar />
        iconlabel small
      </IconButton>
      <IconButton iconlabel>
        <IconCalendar />
        iconlabel medium
      </IconButton>
      <IconButton buttonSize="large" iconlabel>
        <IconCalendar />
        iconlabel large
      </IconButton>
      <PreviewDivider />
      <IconOutlineButton buttonSize="small" iconlabel>
        <IconCalendar />
        outline iconlabel small
      </IconOutlineButton>
      <IconOutlineButton iconlabel>
        <IconCalendar />
        outline iconlabel medium
      </IconOutlineButton>
      <IconOutlineButton buttonSize="large" iconlabel>
        <IconCalendar />
        outline iconlabel large
      </IconOutlineButton>
      <PreviewDivider />
      <IconGhostButton buttonSize="small" iconlabel>
        <IconCalendar />
        ghost iconlabel small
      </IconGhostButton>
      <IconGhostButton iconlabel>
        <IconCalendar />
        ghost iconlabel medium
      </IconGhostButton>
      <IconGhostButton buttonSize="large" iconlabel>
        <IconCalendar />
        ghost iconlabel large
      </IconGhostButton>
      <PreviewDivider />
      <IconButton buttonSize="small">
        <IconCalendar />
      </IconButton>
      <IconButton>
        <IconCalendar />
      </IconButton>
      <IconButton buttonSize="large">
        <IconCalendar />
      </IconButton>
      <PreviewDivider />
      <IconOutlineButton buttonSize="small">
        <IconCalendar />
      </IconOutlineButton>
      <IconOutlineButton>
        <IconCalendar />
      </IconOutlineButton>
      <IconOutlineButton buttonSize="large">
        <IconCalendar />
      </IconOutlineButton>
      <PreviewDivider />
      <IconGhostButton buttonSize="small">
        <IconCalendar />
      </IconGhostButton>
      <IconGhostButton>
        <IconCalendar />
      </IconGhostButton>
      <IconGhostButton buttonSize="large">
        <IconCalendar />
      </IconGhostButton>
    </>
  </Story>
</Preview>

## links

For inline links in paragraphs and informative texts. Regular links navigate to other pages or external content.

Mouseover and click to view `:hover` and `:active` states.

<Preview>
  <Story name="links">
    <>
      <Link id="link1">primary font - medium size</Link>
      <Link id="link2" font="secondary" fontSize="small">
        secondary font - small size
      </Link>
    </>
  </Story>
</Preview>

## buttons in `<a>` elements

Although our button components are `<button>` elements by default, you may also use them as `<a>` elements when semantically recommended by passing the prop `as="a"`. Note that some browsers may render them slightly differently.

If your `<a>` button triggers in-page functionalities instead of linking (to other pages or within the current page), make them semantically accessible by adding a `role="button"`.

<Preview>
  <Story name="buttons in <a> elements">
    <>
      <Button as="a" variant="primary.uranus">
        link button
      </Button>
    </>
  </Story>
</Preview>
