---
title: buttons
---

import { Link } from 'gatsby';
import {
  Button,
  OutlineButton,
  IconButton,
  IconOutlineButton,
  IconGhostButton,
  IconLinkButton,
  Text,
} from '@magnetis/astro-galaxy-components';
import { IconCalendar } from '@magnetis/astro-galaxy-icons';
import Playground from '../components/Playground';
import PropsTableWrapper from '../components/PropsTable';

<Text font="secondary" fontSize="large" color="moon400" marginBottom={5}>
  Buttons define what action will occur when the user interacts with features and components of the
  platform. Buttons are used to initialize and dictate the actions on the interface, either in the
  background or foreground of an experience.
</Text>

## primary buttons

Primary buttons are medium sized and have a colored background. Use these for the main action in a section or screen. You should display only one primary button at a time.

To create a primary button, import `Button` component and use `variant` prop with `primary.uranus` value. You can replace `uranus` with other set of colors.

To modify button size you may use `buttonSize` prop with `small`, `medium` or `large` value. Default value is `medium`. Check the table below for more props information.

Mouseover and click to view `:hover` and `:active` states.

<Playground
  scope={{ Button }}
  code={`
    <Button variant="primary.uranus">
      uranus
    </Button>
    <Button variant="primary.earth">
      earth
    </Button>
    <Button variant="primary.venus">
      venus
    </Button>
    <Button variant="primary.mars">
      mars
    </Button>
    <Button disabled>
      disabled
    </Button>
  `}
/>

<Text as="h3">sizes</Text>

<Playground
  scope={{ Button }}
  code={`
    <Button buttonSize="small">
      small
    </Button>
    <Button buttonSize="medium">
      medium
    </Button>
    <Button buttonSize="large">
      large
    </Button>
  `}
/>

<Text as="h3">props</Text>

<PropsTableWrapper>
  <table>
    <thead>
      <tr>
        <th>property</th>
        <th>PropType</th>
        <th>required</th>
        <th>default</th>
        <th>description</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>children</td>
        <td>node</td>
        <td>no</td>
        <td></td>
        <td></td>
      </tr>
      <tr>
        <td>as</td>
        <td>string</td>
        <td>no</td>
        <td></td>
        <td>Change component's tag.</td>
      </tr>
      <tr>
        <td>variant</td>
        <td>string</td>
        <td>no</td>
        <td>
          <code>primary.uranus</code>
        </td>
        <td>
          Change button variant. "primary.uranus", "primary.earth", "primary.venus", "primary.mars"
        </td>
      </tr>
      <tr>
        <td>buttonSize</td>
        <td>string</td>
        <td>no</td>
        <td>
          <code>medium</code>
        </td>
        <td>Change button size. "small", "medium", "large".</td>
      </tr>
      <tr>
        <td>disabled</td>
        <td>bool</td>
        <td>no</td>
        <td>
          <code>false</code>
        </td>
        <td>Disable button</td>
      </tr>
    </tbody>
  </table>
</PropsTableWrapper>

## outline buttons

Outline buttons can be used as secondary roles. Secondary buttons present secondary actions when there's already a primary action in the same section or screen.

To create a outline button, import `OutlineButton` component and use `variant` prop with `outline.uranus` value. You can replace uranus with other set of colors.

To modify button size you may use `buttonSize` prop with `small`, `medium` or `large` value. Default value is `medium`. Check the table below for more props information.

Mouseover and click to view `:hover` and `:active` states.

<Playground
  scope={{ OutlineButton }}
  code={`
    <OutlineButton variant="outline.uranus">
      outline uranus
    </OutlineButton>
    <OutlineButton variant="outline.earth">
      outline earth
    </OutlineButton>
    <OutlineButton variant="outline.venus">
      outline venus
    </OutlineButton>
    <OutlineButton variant="outline.mars">
      outline mars
    </OutlineButton>
    <OutlineButton disabled>
      outline disabled
    </OutlineButton>
  `}
/>

<Text as="h3">sizes</Text>

<Playground
  scope={{ OutlineButton }}
  code={`
    <OutlineButton buttonSize="small">
      outline small
    </OutlineButton>
    <OutlineButton buttonSize="medium">
      outline medium
    </OutlineButton>
    <OutlineButton buttonSize="large">
      outline large
    </OutlineButton>
  `}
/>

<Text as="h3">props</Text>

<PropsTableWrapper>
  <table>
    <thead>
      <tr>
        <th>property</th>
        <th>PropType</th>
        <th>required</th>
        <th>default</th>
        <th>description</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>children</td>
        <td>node</td>
        <td>no</td>
        <td></td>
        <td></td>
      </tr>
      <tr>
        <td>as</td>
        <td>string</td>
        <td>no</td>
        <td></td>
        <td>Change component's tag.</td>
      </tr>
      <tr>
        <td>variant</td>
        <td>string</td>
        <td>no</td>
        <td>
          <code>outline.uranus</code>
        </td>
        <td>
          Change button variant. "outline.uranus", "outline.earth", "outline.venus", "outline.mars"
        </td>
      </tr>
      <tr>
        <td>buttonSize</td>
        <td>string</td>
        <td>no</td>
        <td>
          <code>medium</code>
        </td>
        <td>Change button size. "small", "medium", "large".</td>
      </tr>
      <tr>
        <td>disabled</td>
        <td>bool</td>
        <td>no</td>
        <td>
          <code>false</code>
        </td>
        <td>Disable button</td>
      </tr>
    </tbody>
  </table>
</PropsTableWrapper>

## icon buttons

Icon buttons can be used for simple microinteractions and commands.

To create an icon button, import `IconButton` component and use `variant` prop with `primary.uranus` value. Then, add an icon component inside the icon button. See our <Link to="/iconography">Iconography</Link> to learn how to use Astro icons.

To modify button size you may use `buttonSize` prop with `small`, `medium` or `large` value. Default value is `medium`. Check the table below for more props information.

Mouseover and click to view `:hover` and `:active` states.

<Playground
  scope={{ IconButton, IconCalendar }}
  code={`
    <IconButton variant="primary.uranus">
      <IconCalendar />
    </IconButton>
    <IconButton variant="primary.earth">
      <IconCalendar />
    </IconButton>
    <IconButton variant="primary.venus">
      <IconCalendar />
    </IconButton>
    <IconButton variant="primary.mars">
      <IconCalendar />
    </IconButton>
    <IconButton disabled>
      <IconCalendar />
    </IconButton>
  `}
/>

<Text as="h3">sizes</Text>

<Playground
  scope={{ IconButton, IconCalendar }}
  code={`
    <IconButton buttonSize="small">
      <IconCalendar />
    </IconButton>
    <IconButton buttonSize="medium">
      <IconCalendar />
    </IconButton>
    <IconButton buttonSize="large">
      <IconCalendar />
    </IconButton>
  `}
/>

<Text as="h3">props</Text>

Refer to iconlabel buttons below.

## iconlabel buttons

To create an iconlabel button, first create a regular `IconButton`, plus the `iconlabel` prop. Then, if you want the icon to the left of the label, add an icon component inside the button and before its inner text. See our <Link to="/iconography">Iconography</Link> to learn how to use Astro icons.

If you want the icon to the right of the label, add the `iconlabelRight` prop in addition to the previous props and place the icon component inside the button and after its inner text, instead.

To modify button size you may use `buttonSize` prop with `small`, `medium` or `large` value. Default value is `medium`. Check the table below for more props information.

Mouseover and click to view `:hover` and `:active` states.

<Playground
  scope={{ IconButton, IconCalendar }}
  code={`
    <IconButton iconlabel>
      <IconCalendar />
      iconlabel
    </IconButton>
    
    <IconButton iconlabel iconlabelRight>
      iconlabel right
      <IconCalendar />
    </IconButton>
    
    <IconButton iconlabel disabled>
      <IconCalendar />
      iconlabel disabled
    </IconButton>
  `}
/>

<Text as="h3">sizes</Text>

<Playground
  scope={{ IconButton, IconCalendar }}
  code={`
    <IconButton iconlabel buttonSize="small">
      <IconCalendar />
      iconlabel small
    </IconButton>
    <IconButton iconlabel buttonSize="medium">
      <IconCalendar />
      iconlabel medium
    </IconButton>
    <IconButton iconlabel buttonSize="large">
      <IconCalendar />
      iconlabel large
    </IconButton>
  `}
/>

<Text as="h3">props</Text>

<PropsTableWrapper>
  <table>
    <thead>
      <tr>
        <th>property</th>
        <th>PropType</th>
        <th>required</th>
        <th>default</th>
        <th>description</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>children</td>
        <td>node</td>
        <td>no</td>
        <td></td>
        <td></td>
      </tr>
      <tr>
        <td>as</td>
        <td>string</td>
        <td>no</td>
        <td></td>
        <td>Change component's tag.</td>
      </tr>
      <tr>
        <td>variant</td>
        <td>string</td>
        <td>no</td>
        <td>
          <code>primary.uranus</code>
        </td>
        <td>
          Change button variant. "primary.uranus", "primary.earth", "primary.venus", "primary.mars"
        </td>
      </tr>
      <tr>
        <td>buttonSize</td>
        <td>string</td>
        <td>no</td>
        <td>
          <code>medium</code>
        </td>
        <td>Change button size. "small", "medium", "large".</td>
      </tr>
      <tr>
        <td>disabled</td>
        <td>bool</td>
        <td>no</td>
        <td>
          <code>false</code>
        </td>
        <td>Disable button</td>
      </tr>
      <tr>
        <td>iconlabel</td>
        <td>boolean</td>
        <td>no</td>
        <td>
          <code>false</code>
        </td>
        <td>Add inner text.</td>
      </tr>
      <tr>
        <td>iconlabelRight</td>
        <td>boolean</td>
        <td>no</td>
        <td>
          <code>false</code>
        </td>
        <td>Flip icon position to the right.</td>
      </tr>
    </tbody>
  </table>
</PropsTableWrapper>

## icon outline buttons

Icon outline buttons are commonly used in microinteractions and secondary commands.

To create an icon outline button import `IconOutlineButton` component and use `variant` prop with `outline.uranus` value. Then, add an icon component inside the icon button. See our <Link to="/iconography">Iconography</Link> to learn how to use Astro icons.

To modify button size you may use `buttonSize` prop with `small`, `medium` or `large` value. Default value is `medium`. Check the table below for more props information.

Mouseover and click to view `:hover` and `:active` states.

<Playground
  scope={{ IconOutlineButton, IconCalendar }}
  code={`
    <IconOutlineButton variant="outline.uranus">
      <IconCalendar />
    </IconOutlineButton>
    <IconOutlineButton variant="outline.earth">
      <IconCalendar />
    </IconOutlineButton>
    <IconOutlineButton variant="outline.venus">
      <IconCalendar />
    </IconOutlineButton>
    <IconOutlineButton variant="outline.mars">
      <IconCalendar />
    </IconOutlineButton>
    <IconOutlineButton disabled>
      <IconCalendar />
    </IconOutlineButton>
  `}
/>

<Text as="h3">sizes</Text>

<Playground
  scope={{ IconOutlineButton, IconCalendar }}
  code={`
    <IconOutlineButton buttonSize="small">
      <IconCalendar />
    </IconOutlineButton>
    <IconOutlineButton buttonSize="medium">
      <IconCalendar />
    </IconOutlineButton>
    <IconOutlineButton buttonSize="large">
      <IconCalendar />
    </IconOutlineButton>
  `}
/>

<Text as="h3">props</Text>

Refer to iconlabel outline buttons buttons below.

## iconlabel outline buttons

Iconlabel outline buttons are commonly used in microinteractions and secondary commands.

To create an iconlabel outline button, first create a regular `IconOutlineButton`, plus the `iconlabel` prop. Then, if you want the icon to the left of the label, add an icon component inside the button and before its inner text. See our <Link to="/iconography">Iconography</Link> to learn how to use Astro icons.

If you want the icon to the right of the label, add the `iconlabelRight` prop in addition to the previous props and place the icon component inside the button and after its inner text, instead.

To modify button size you may use `buttonSize` prop with `small`, `medium` or `large` value. Default value is `medium`. Check the table below for more props information.

Mouseover and click to view `:hover` and `:active` states.

<Playground
  scope={{ IconOutlineButton, IconCalendar }}
  code={`
    <IconOutlineButton iconlabel>
      <IconCalendar />
      iconlabel outline
    </IconOutlineButton>
    <IconOutlineButton iconlabel iconlabelRight>
      iconlabel right outline
      <IconCalendar />
    </IconOutlineButton>
    <IconOutlineButton iconlabel disabled>
      <IconCalendar />
      iconlabel outline disabled
    </IconOutlineButton>
  `}
/>

<Text as="h3">sizes</Text>

<Playground
  scope={{ IconOutlineButton, IconCalendar }}
  code={`
    <IconOutlineButton iconlabel buttonSize="small">
      <IconCalendar />
      iconlabel outline small
    </IconOutlineButton>
    <IconOutlineButton iconlabel buttonSize="medium">
      <IconCalendar />
      iconlabel outline medium
    </IconOutlineButton>
    <IconOutlineButton iconlabel buttonSize="large">
      <IconCalendar />
      iconlabel outline large
    </IconOutlineButton>
  `}
/>

<Text as="h3">props</Text>

<PropsTableWrapper>
  <table>
    <thead>
      <tr>
        <th>property</th>
        <th>PropType</th>
        <th>required</th>
        <th>default</th>
        <th>description</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>children</td>
        <td>node</td>
        <td>no</td>
        <td></td>
        <td></td>
      </tr>
      <tr>
        <td>as</td>
        <td>string</td>
        <td>no</td>
        <td></td>
        <td>Change component's tag.</td>
      </tr>
      <tr>
        <td>variant</td>
        <td>string</td>
        <td>no</td>
        <td>
          <code>outline.uranus</code>
        </td>
        <td>
          Change button variant. "outline.uranus", "outline.earth", "outline.venus", "outline.mars"
        </td>
      </tr>
      <tr>
        <td>buttonSize</td>
        <td>string</td>
        <td>no</td>
        <td>
          <code>medium</code>
        </td>
        <td>Change button size. "small", "medium", "large".</td>
      </tr>
      <tr>
        <td>disabled</td>
        <td>bool</td>
        <td>no</td>
        <td>
          <code>false</code>
        </td>
        <td>Disable button</td>
      </tr>
      <tr>
        <td>iconlabel</td>
        <td>boolean</td>
        <td>no</td>
        <td>
          <code>false</code>
        </td>
        <td>Add inner text.</td>
      </tr>
      <tr>
        <td>iconlabelRight</td>
        <td>boolean</td>
        <td>no</td>
        <td>
          <code>false</code>
        </td>
        <td>Flip icon position to the right.</td>
      </tr>
    </tbody>
  </table>
</PropsTableWrapper>

## icon ghost buttons

Icon Ghost buttons can be used for simple microinteractions and commands.

To create an ghost icon button import `IconGhostButton` component and use `variant` prop with `ghost.uranus` value. Then, add an icon component inside the icon button. See our <Link to="/iconography">Iconography</Link> to learn how to use Astro icons.

To modify button size you may use `buttonSize` prop with `small`, `medium` or `large` value. Default value is `medium`. Check the table below for more props information.

Mouseover and click to view `:hover` and `:active` states.

<Playground
  scope={{ IconGhostButton, IconCalendar }}
  code={`
    <IconGhostButton variant="ghost.uranus">
      <IconCalendar />
    </IconGhostButton>
    <IconGhostButton variant="ghost.earth">
      <IconCalendar />
    </IconGhostButton>
    <IconGhostButton variant="ghost.venus">
      <IconCalendar />
    </IconGhostButton>
    <IconGhostButton variant="ghost.mars">
      <IconCalendar />
    </IconGhostButton>
    <IconGhostButton disabled>
      <IconCalendar />
    </IconGhostButton>
  `}
/>

<Text as="h3">sizes</Text>

<Playground
  scope={{ IconGhostButton, IconCalendar }}
  code={`
    <IconGhostButton buttonSize="small">
      <IconCalendar />
    </IconGhostButton>
    <IconGhostButton buttonSize="medium">
      <IconCalendar />
    </IconGhostButton>
    <IconGhostButton buttonSize="large">
      <IconCalendar />
    </IconGhostButton>
  `}
/>

<Text as="h3">props</Text>

Refer to ghost iconlabel buttons below.

## iconlabel ghost buttons

Iconlabel Ghost buttons are also used in microinteractions and smaller commands.

To create an ghost iconlabel button, first create a regular `IconGhostButton`, plus the `iconlabel` prop. Then, if you want the icon to the left of the label, add an icon component inside the button and before its inner text. See our <Link to="/iconography">Iconography</Link> to learn how to use Astro icons.

If you want the icon to the right of the label, add the `iconlabelRight` prop in addition to the previous props and place the icon component inside the button and after its inner text, instead.

To modify button size you may use `buttonSize` prop with `small`, `medium` or `large` value. Default value is `medium`. Check the table below for more props information.

Mouseover and click to view `:hover` and `:active` states.

<Playground
  scope={{ IconGhostButton, IconCalendar }}
  code={`
    <IconGhostButton variant="ghost.uranus" iconlabel>
      <IconCalendar />
      uranus
    </IconGhostButton>
    <IconGhostButton variant="ghost.earth" iconlabel>
      <IconCalendar />
      earth
    </IconGhostButton>
    <IconGhostButton variant="ghost.venus" iconlabel>
      <IconCalendar />
      venus
    </IconGhostButton>
    <IconGhostButton variant="ghost.mars" iconlabel>
      <IconCalendar />
      mars
    </IconGhostButton>
    <IconGhostButton iconlabel disabled>
      <IconCalendar />
      mars
    </IconGhostButton>
  `}
/>

<Text as="h3">sizes</Text>

<Playground
  scope={{ IconGhostButton, IconCalendar }}
  code={`
    <IconGhostButton iconlabel buttonSize="small">
      <IconCalendar />
      ghost small
    </IconGhostButton>
    <IconGhostButton iconlabel buttonSize="medium">
      <IconCalendar />
      ghost medium
    </IconGhostButton>
    <IconGhostButton iconlabel buttonSize="large">
      <IconCalendar />
      ghost large
    </IconGhostButton>
  `}
/>

<Text as="h3">props</Text>

<PropsTableWrapper>
  <table>
    <thead>
      <tr>
        <th>property</th>
        <th>PropType</th>
        <th>required</th>
        <th>default</th>
        <th>description</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>children</td>
        <td>node</td>
        <td>no</td>
        <td></td>
        <td></td>
      </tr>
      <tr>
        <td>as</td>
        <td>string</td>
        <td>no</td>
        <td></td>
        <td>Change component's tag.</td>
      </tr>
      <tr>
        <td>variant</td>
        <td>string</td>
        <td>no</td>
        <td>
          <code>ghost.uranus</code>
        </td>
        <td>Change button variant. "ghost.uranus", "ghost.earth", "ghost.venus", "ghost.mars"</td>
      </tr>
      <tr>
        <td>buttonSize</td>
        <td>string</td>
        <td>no</td>
        <td>
          <code>medium</code>
        </td>
        <td>Change button size. "small", "medium", "large".</td>
      </tr>
      <tr>
        <td>disabled</td>
        <td>bool</td>
        <td>no</td>
        <td>
          <code>false</code>
        </td>
        <td>Disable button</td>
      </tr>
      <tr>
        <td>iconlabel</td>
        <td>boolean</td>
        <td>no</td>
        <td>
          <code>false</code>
        </td>
        <td>Add inner text.</td>
      </tr>
      <tr>
        <td>iconlabelRight</td>
        <td>boolean</td>
        <td>no</td>
        <td>
          <code>false</code>
        </td>
        <td>Flip icon position to the right.</td>
      </tr>
    </tbody>
  </table>
</PropsTableWrapper>

## icon link buttons

Icon link buttons can be used for menu items and simple interactions or commands.

To create an icon link button, import `IconLinkButton`. Then, if you want the icon to the left of the label, add an icon component inside the button and before its inner text. See our <Link to="/iconography">Iconography</Link> to learn how to use Astro icons.

If you want the icon to the right of the label, add the `iconlabelRight` prop in addition to the previous props and place the icon component inside the button and after its inner text, instead.

To create an icon link button with bold text, just add the `bold` prop.

Mouseover and click to view `:hover` and `:active` states.

<Playground
  dark
  scope={{ IconLinkButton, IconCalendar }}
  code={`
    <IconLinkButton>
      <IconCalendar />
      icon link
    </IconLinkButton>
    <IconLinkButton bold>
      <IconCalendar />
      icon link bold
    </IconLinkButton>
    <IconLinkButton iconlabelRight>
      icon link right
      <IconCalendar />
    </IconLinkButton>
    <IconLinkButton disabled>
      <IconCalendar />
      disabled
    </IconLinkButton>
  `}
/>

<Text as="h3">props</Text>

<PropsTableWrapper>
  <table>
    <thead>
      <tr>
        <th>property</th>
        <th>PropType</th>
        <th>required</th>
        <th>default</th>
        <th>description</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>children</td>
        <td>node</td>
        <td>no</td>
        <td></td>
        <td></td>
      </tr>
      <tr>
        <td>as</td>
        <td>string</td>
        <td>no</td>
        <td></td>
        <td>Change component's tag.</td>
      </tr>
      <tr>
        <td>buttonSize</td>
        <td>string</td>
        <td>no</td>
        <td>
          <code>medium</code>
        </td>
        <td>
          Change button size. "small", "medium", "large". Although supported, small and large are
          not on our official collection for this button.
        </td>
      </tr>
      <tr>
        <td>disabled</td>
        <td>bool</td>
        <td>no</td>
        <td>
          <code>false</code>
        </td>
        <td>Disable button</td>
      </tr>
      <tr>
        <td>iconlabel</td>
        <td>boolean</td>
        <td>no</td>
        <td>
          <code>true</code>
        </td>
        <td>Add inner text.</td>
      </tr>
      <tr>
        <td>iconlabelRight</td>
        <td>boolean</td>
        <td>no</td>
        <td>
          <code>false</code>
        </td>
        <td>Flip icon position to the right.</td>
      </tr>
      <tr>
        <td>bold</td>
        <td>boolean</td>
        <td>no</td>
        <td>
          <code>false</code>
        </td>
        <td>Change font weight to bold.</td>
      </tr>
    </tbody>
  </table>
</PropsTableWrapper>

## buttons in `<a>` elements

Although our button components are `<button>` elements by default, you may also use them as `<a>` elements when semantically recommended by passing the prop `as="a"`. Note that some browsers may render them slightly differently.

If your `<a>` button triggers in-page functionalities instead of linking (to other pages or within the current page), make them semantically accessible by adding a `role="button"`.

<Playground
  scope={{ Button }}
  code={`
    <Button as="a" variant="primary.uranus">
      link button
    </Button>
  `}
/>
